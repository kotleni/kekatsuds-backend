# KekatsuDS Backend
Custom backend for Kekatsu-DS to download ROMs from folders.

## ROMs placement
Put all of our roms in `roms/X/` folder, where is `X` means console id. Check `server.py` for more details.
